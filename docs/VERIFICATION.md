# Verification record

The initial build was checked on Linux with GCC and WASI SDK 25 (Clang 19), Python 3.12, and Node 22.

## Automated checks

- Native behavioral tests: stationary jump's exact initial trajectory and 242-unit peak; release-height reduction; button-edge behavior; stick deadzone/nonlinearity; walking acceleration and speed oscillation; double/triple jump chain; long jump; backflip; turnaround side somersault; speed-dependent dive/kick; ground pound; wall collision/kick; replay determinism; invalid input/geometry rejection.
- Native/WASM parity: all **80 bytes of the public state**, compared on **every frame of 360-frame input streams in each of ten areas** (3,600 ticks). The stream includes walking, directional changes, jumps, crouching and attacks.
- Ledge regression: actual descending jumps into the new garden, followed by neutral hang, toward-platform slow climb, sideways slow climb, away/drop, A/quick climb, and Z/drop. Repeated with four camera orientations in both native C and WASM, comparing every state byte for another **1,992 ticks**. Toward/sideways starts climbing after the original ten-tick hang delay; away and Z release the grip. The reported all-directions-drop issue was not reproduced.
- Course traversal: real controller inputs ascend the 1,550-unit wall-kick shaft, grab its rim, pull up, and walk onto the rear terrace.
- New-route traversal: every garden/skyline hop has multiple successful controller timings, and the canopy can be caught, traversed, and all three sparks collected. Twelve render groups cover all 528 collision triangles; coordinates remain within 7,200 units.
- Animation library: all 209 metadata records and secondary pose selectors are checked across phases and movement states.
- Presentation: hanging torso below the ledge, gloves anchored in world space through the initial pull-up, finite deterministic poses without state mutation, camera-relative climb cues, and idempotent collectible progress/reset.
- Frame scheduling: identical 300-tick end state at simulated 30, 60 and 144 display frames per second.
- Level checks: all ten destination spawns settle on the floor generated by their visible triangles.
- Pause clock clearing and long-stall catch-up limit.
- Cinder Caldera (`tests/caldera.test.mjs`):
  - Geometry integrity: capacity, integer coordinates, no degenerate triangles, lava only on the floor, grates only as ceilings.
  - Every checkpoint settles on its floor.
  - The lava boost costs exactly three wedges and a coin heals one.
  - Native/WASM parity: all 80 state bytes, compared on every tick of 300-tick random input streams from each of the five checkpoints. Coin heals are mixed in (1,500 ticks).
  - Route proofs: controller-input timing searches through the actual core complete every leg of the main route. That covers the steps and long jump, the rampart and backflip step, the 1,160-unit chimney kicks, both hang traverses, the ridge beams and blades, the chute, the Spire's kick corridor and backflip step, and the summit. They also reach all eight shards and the altar.
  - Negative checks: the anvil roof, the belfry, and the altar can't be reached with a plain jump.
- Heal hook: a native test shows lava costs 0x300 health, `s64_heal` restores exactly one wedge per 4 units, and health caps at 0x880.
- Presentation:
  - Live crossfades blend and then settle exactly; keys changing on consecutive ticks still converge.
  - The side-flip landing turns gradually back to the core's facing.
  - The slide kick is low and feet-first; decelerating keeps striding.
  - The ground-pound landing matches its seated recovery; fall-damage knockbacks topple.

Commands are in the README. The cross-compiler test runs the same inputs through the actual native shared library and committed WASM, not a JavaScript rewrite of physics.

## Practical limits

Third edition: the level, HUD, intro shot, and offline single-file package were exercised in headless Chromium using software WebGL (screenshots only; no human play session or physical gamepad). The connected browser in the development environment previously rejected local HTTP previews with `ERR_BLOCKED_BY_CLIENT`. The user played and confirmed the first edition works well. The second edition's rig transforms and movement are exercised headlessly, but that does not verify WebGL rendering or physical gamepad behavior. A browser-interaction/visual pass on this edition is still needed. CI tests the simulation and packages the offline browser demo.

These tests establish internal consistency and specific upstream behaviors. They do not establish N64 equivalence: no reference console/emulator trace was available. See [FIDELITY.md](FIDELITY.md) before using the word “exact.”
